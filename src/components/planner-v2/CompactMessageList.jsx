// Re-export so the existing import path keeps working after the decomposition
// into ./compactMessageList/{components,hooks,lib}/. See AUDIT.md §8 / PR 4.
export { default } from './compactMessageList/CompactMessageList.jsx';
