// Re-export so the existing import path keeps working after the decomposition
// into ./routeMapColumn/{components,hooks,lib}/. See AUDIT.md §8 / PR 5.
export { default } from './routeMapColumn/RouteMapColumn.jsx';
