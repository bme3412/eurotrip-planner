// Re-export so the existing import path keeps working after the decomposition
// into ./monthlyTabbedView/{components,hooks,lib}/. See AUDIT.md §8 / PR 3.
export { default } from './monthlyTabbedView/MonthlyTabbedView.jsx';
