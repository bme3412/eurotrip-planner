// Re-export so the existing import path keeps working after the decomposition
// into ./neighborhoodsList/{components,hooks,lib}/. See AUDIT.md §8 / PR 2.
export { default } from './neighborhoodsList/NeighborhoodsList.jsx';
